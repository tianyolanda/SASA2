_BASE_CONFIG_: cfgs/dense_models/3dssd_dsasa.yaml

MODEL:
    DENSITY_CONFIG:
        MODE: fixed
        RADIUS: 0.15
        MAX_NSAMPLE: 8
        NORM_MAX: 8
        OUTPUT_NORMALIZED: False

    BACKBONE_3D:
        SA_CONFIG:
            DENSITY_NORM_MAX: 8
            DENSITY_NORMALIZED: False

