_BASE_CONFIG_: cfgs/dense_models/3dssd_dsasa.yaml

DATA_CONFIG:
    _BASE_CONFIG_: cfgs/dataset_configs/dense_dataset.yaml

MODEL:
    DENSITY_CONFIG:
        MODE: fixed
        RADIUS: 0.25
        MAX_NSAMPLE: 12
        NORM_MAX: 12
        OUTPUT_NORMALIZED: False

    BACKBONE_3D:
        SA_CONFIG:
            DENSITY_WEIGHT_MODE: gated_residual
            DENSITY_ALPHA: 0.2
            DENSITY_GATE_TAU: 0.3
            DENSITY_GATE_K: 10.0
            DENSITY_NORM_MAX: 12
            DENSITY_NORMALIZED: False

